#
# PAM configuration file for the i3lock-color screen locker — deployed by
# eos-bootstrap (fingerprint role) so drift self-heals. Deliberately
# stock/password-only: lock-screen fingerprint unlock was removed 2026-08-07
# after the FT9366 + TOD driver wedged permanently on this unit (see role
# task comments).
#

auth include system-local-login # For Arch/Gentoo
#auth include login # For Debian
