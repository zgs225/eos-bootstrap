---
- name: Install pacman packages
  community.general.pacman:
    name: "{{ pacman_packages }}"
    state: present
    update_cache: true

- name: Remove pacman packages owned by mise
  community.general.pacman:
    name: "{{ packages_pacman_absent }}"
    state: absent
  when: packages_pacman_absent | length > 0
