# {{ ansible_managed }}
# Allow any user to start the X server — required for xrdp's
# xorgxrdp backend since sessions are launched by xrdp-sesman,
# not by a physical console login.

allowed_users = anybody
