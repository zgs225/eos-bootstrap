# Default scheduler loaded by scx_loader at boot.
# Alternatives: scx_rusty (CFS-like), scx_layered (custom layers),
# scx_bpfland, scx_cosmos. Restart scx_loader.service after changing.
default_sched = "scx_lavd"
